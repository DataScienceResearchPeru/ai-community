# GitHub setup

An organization owner should confirm the repository is public, apply its description and topics, enable Discussions, and create the proposed teams. Run `scripts/github/configure-repository.sh --dry-run` first, review its output, then rerun without `--dry-run` only after confirming plan and permissions.
