# Scripts

Scripts are safeguards for repeatable GitHub administration. Copy `community.config.example.env` to a local, ignored configuration file and export only non-secret values. Always start with `--dry-run`.
